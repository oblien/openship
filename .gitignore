# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
.next-saas/
.turbo/
out/
# fumadocs generated content source (apps/web/.source) — compiled from source.config.ts
.source/
# Self-hosted server release bundle (apps/api/scripts/build-release.ts)
apps/api/release-dist/
# Desktop staged payload (apps/desktop/build/stage.ts)
apps/desktop/resources/
# CLI README is staged from the root README at pack time (apps/cli prepack)
apps/cli/README.md
# bun build --compile scratch/leftover binaries
*.bun-build
# Build artifacts left behind by running the deploy fixtures locally.
# Scoped to fixtures/deploy/ on purpose: a bare `build/` or `bin/` rule would
# also swallow the tracked apps/desktop/build/ and apps/cli/build/ sources.
fixtures/deploy/*/obj/
fixtures/deploy/*/bin/
fixtures/deploy/*/.gradle/
fixtures/deploy/*/build/

# Environment
.env
.env.local
.env.*.local
.env.saas
.env.local-saas

# Dev-only generated secrets (per-machine, not for prod)
.dev-secrets.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/

# Database (PGlite local data)
.openship/
data/
*.pglite

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
.react-router
.wrangler
next-env.d.ts