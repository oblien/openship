---
title: status
description: Commands, arguments, and options for openship status.
cliGroup: openship status
---

See the [CLI overview](/docs/cli) for examples. Global flags such as `--json` go before the command; see [global options](/docs/cli/reference#global-options).

## openship status

Show the local Openship service (installed/running, ports) and the active context's API health

```text
openship status [options]
```
