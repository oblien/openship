---
title: stop
description: Commands, arguments, and options for openship stop.
cliGroup: openship stop
---

See the [CLI overview](/docs/cli) for examples. Global flags such as `--json` go before the command; see [global options](/docs/cli/reference#global-options).

## openship stop

Stop Openship (started by `openship up`) — it won't restart or return on reboot

```text
openship stop [options]
```
