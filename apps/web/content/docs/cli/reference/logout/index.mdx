---
title: logout
description: Commands, arguments, and options for openship logout.
cliGroup: openship logout
---

See the [workflow guide](/docs/cli/access) for examples. Global flags such as `--json` go before the command; see [global options](/docs/cli/reference#global-options).

## openship logout

Remove the stored Openship token

```text
openship logout [options]
```

| Option | Meaning |
| --- | --- |
| `--context <name>` | Log out of a specific context (defaults to active) |
